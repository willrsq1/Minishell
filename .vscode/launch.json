{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "cppdbg",
            "name": "Debug shell",
            "request": "launch",
            "program": "${workspaceFolder}/minishell",
            "args": [
            ],
            "cwd": "${workspaceFolder}",
        }
    ]
}